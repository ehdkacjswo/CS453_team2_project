import math 

def function2(x, y, z, a, b, c):
    u = x + a
    w = y + c
    m = z + b
    if (x > -10):
        if (x <= 0):
            if (x >= y - 100):
                if (y < math.sqrt(100001)):
                    if (y > 99):
                        if (z > 999):
                            if (z < 1000.5):
                                if (z == x + 1000):
                                    pass
            else:
                if (y < math.sqrt(100001)):
                    if (y > 99):
                        if (z > 999):
                            if (z < 1000.5):
                                if (z == x + 1000):
                                    pass
    if (b < 111):
        if (b > 99):
            if (b <= a + 100):
                if (c <= a + 1001):
                    if (c > 999):
                        if (c < 1000.5):
                            if (a > 10):
                                if ( a <= 10.5):
                                    pass
            else:
                if (c <= a + 1001):
                    if (c > 999):
                        if (c < 1000.5):
                            if (a > 10):
                                if ( a <= 10.5):
                                    pass
    elif (m > 1010):
        u += m - 1000
        if (m <= 1010.35):
            if (m <= 1000 + x - 90):
                if (u > -1):
                    u += 50
                    if (u <= 100):
                        w = u - m
                        if (u < w - 99.46):
                            pass
